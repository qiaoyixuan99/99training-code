# 闲鱼自动发货辅助系统 · 依赖包
# 安装方式: pip install -r requirements.txt
#
# 当前版本仅使用 Python 标准库，以下为可选依赖：

# 如需检查网盘链接有效性（百度网盘）
# requests>=2.28.0

# 如需生成二维码分享链接
# qrcode>=7.3

# 如需发送邮件通知（新订单提醒）
# smtplib (标准库，无需安装)

# 当前核心功能无需额外安装
# pip install -r requirements.txt 可以正常运行
